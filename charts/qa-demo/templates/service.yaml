apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa-demo.name" . }}
  labels:
    {{- include "qa-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "qa-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
